const sides = 4;
export class SquareDrawer {
    async draw(data) {
        const { drawSquare } = await import("./Utils.js");
        drawSquare(data);
    }
    getSidesCount() {
        return sides;
    }
}
