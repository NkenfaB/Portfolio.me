import { type IParticleMover, type Particle } from "@tsparticles/engine";
export declare class ParallaxMover implements IParticleMover {
    init(): Promise<void>;
    isEnabled(particle: Particle): boolean;
    move(particle: Particle): Promise<void>;
}
