const sides = 1;
export class LineDrawer {
    async draw(data) {
        const { drawLine } = await import("./Utils.js");
        drawLine(data);
    }
    getSidesCount() {
        return sides;
    }
}
